jobs:
  build:
    on:
      push:
        branches:
          - sonnar
    runs-on: ubuntu-latest
    steps:
      - name: Afficher les valeurs des tokens
        run: |
          echo "GITHUB_TOKEN: $GITHUB_TOKEN"
          echo "SONAR_TOKEN: $SONAR_TOKEN"
